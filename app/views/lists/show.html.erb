<h1>coucou</h1>

<%= @list.name %>

<%= link_to 'Go back', lists_path %>

<% @list.bookmarks.each do |bookmark| %>
  <%= bookmark.comment %>
  <%= link_to "Delete", bookmark_path(bookmark),
   data: {turbo_method: :delete, turbo_confirm: "Are you Ok ?"} %>
<% end %>
